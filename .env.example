# GCP Configuration
PROJECT_ID=
REGION=us-central1

# PostgreSQL Database
# For local development, use localhost or docker service name
# For production, use your managed PostgreSQL instance (e.g., Neon, Cloud SQL)
DATABASE_USER=default
DATABASE_PASSWORD=password
DATABASE_HOST=localhost
DATABASE_NAME=edupulse

# Confluent Kafka
# For local development, use localhost:9092
# For Confluent Cloud, use your cluster's bootstrap servers
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_API_KEY=
KAFKA_API_SECRET=

# Confluent Schema Registry
SCHEMA_REGISTRY_URL=
SCHEMA_REGISTRY_API_KEY=
SCHEMA_REGISTRY_API_SECRET=

# AI Configuration (Optional)
# For Gemini API key (if not using Vertex AI)
GEMINI_API_KEY=

# JWT Authentication
# Generate with: openssl rand -base64 64
JWT_SIGNING_KEY=
